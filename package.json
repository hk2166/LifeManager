{
  "name": "life-os",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"npm run dev -w api\" \"npm run dev -w web\"",
    "db:migrate": "npm run migrate -w api",
    "db:seed": "npm run seed -w api",
    "db:reset": "npm run reset -w api",
    "extract": "npm run extract -w api",
    "eval:extraction": "npm run eval:extraction -w api",
    "eval:memos": "npm run eval:memos -w api",
    "demo:reset": "npm run db:reset && npm run extract",
    "typecheck": "npm run typecheck -w api && npm run typecheck -w web"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
