# Postgres (docker-compose default)
DATABASE_URL=postgres://postgres:postgres@localhost:5432/lifeos

# Google OAuth - see docs/GOOGLE_SETUP.md
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:3001/oauth2callback

# Anthropic - commitment extraction + memo classification
ANTHROPIC_API_KEY=

# OpenAI - Whisper transcription
OPENAI_API_KEY=

# Owner identity: whose inbox this is (used by extraction prompts and the seed corpus)
DEMO_OWNER_NAME=Hemant
DEMO_OWNER_EMAIL=you@lifeos.demo

# Memo routing: below this confidence, surface a confirmation chip instead of filing silently
CLASSIFY_CONFIDENCE_MIN=0.8

# Auth: secret for signing login tokens (JWT). Use a long random string in production.
JWT_SECRET=change-me-to-a-long-random-string
